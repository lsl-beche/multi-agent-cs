# 电商购物平台后台管理系统 — 全流程技术落地方案

## 一、方案概述

本方案面向**电商购物平台全链路后台管理**，在现有 CSagent（智能客服 Agent）基础上，新增**商品管理、订单管理、用户管理、库存管理、支付管理、物流管理、营销活动、评价管理、数据看板、RBAC权限**等全功能模块，最终形成一套完整的类淘宝电商平台后端管理系统。

**现有基础（CSagent）**：已实现 Supervisor 多 Agent 编排、知识库 RAG 检索、意图识别、槽位填充、合规审查、工具调用，**Agent 链路可跑通**（完成度 ~35%）。

**新增目标**：补全电商业务数据模型（+24张表）、管理后台 API（+50+端点）、运营后台前端，使系统从"客服工具箱"升级为"电商平台全栈后端"。

**CSagent 与管理系统的关系**：
- CSagent **保持不变**，作为管理系统的子模块，继续通过 `/api/chat` 提供智能客服
- 管理后台新增的 Service 层，**CSagent 的工具调用将从中读取真实业务数据**（替代当前模拟数据）
- 两者**共享同一套 PostgreSQL 数据库**，客服回答基于管理后台录入的真实商品/订单/物流信息

**实施周期预估**：现有骨架已完成约 35%，新增模块预计 4-6 个月完成整体落地。

## 二、开发全流程（9大阶段）

### 第一阶段：需求分析与目标定义

#### 1.1 系统功能全景

系统分为**10大功能模块**，CSagent 客服作为其中之一：

| 优先级 | 模块 | 说明 | 状态 |
|--------|------|------|------|
| **P0** | CS Agent 智能客服 | 多Agent问答、RAG检索、订单/售后/物流查询 | ✅ 骨架完成 |
| **P0** | 用户管理 | 注册/登录/认证、收货地址、会员管理 | 🆕 |
| **P0** | 商品管理 | 商品CRUD、类目管理、SKU/规格、图片管理 | 🆕 |
| **P0** | 订单管理 | 订单生命周期、状态流转、多条件筛选、批量操作 | 🆕 |
| **P0** | 库存管理 | 库存CRUD、出入库流水、库存预警、仓库管理 | 🆕 |
| **P0** | RBAC权限 | 角色/权限管理、操作日志 | 🆕 |
| **P1** | 支付管理 | 支付流水、退款审核、渠道配置 | 🆕 |
| **P1** | 物流管理 | 物流单号、轨迹追踪、运费模板、发货管理 | 🆕 |
| **P1** | 营销活动 | 优惠券、满减/秒杀活动、优惠计算引擎 | 🆕 |
| **P1** | 评价管理 | 商品评价审核、客服评价(CSAT) | 🆕 |
| **P2** | 数据看板 | 销售统计、用户分析、客服数据大屏、报表导出 | 🆕 |

#### 1.2 CS Agent 客服场景（已有，保持不变）

按咨询类型占比划分优先级：

| 优先级 | 咨询类型 | 占比 | 典型场景 |
|--------|---------|------|---------|
| P0 | 基础查询类 | 45% | 订单状态、物流信息、退换货政策 |
| P1 | 技术问题类 | 25% | APP使用故障、支付异常 |
| P2 | 促销咨询类 | 20% | 优惠券规则、满减活动 |
| P3 | 投诉建议类 | 10% | 商品质量、服务态度 |

#### 1.3 核心目标指标
- **CS 客服**：应答准确率 ≥ 90%，响应 < 1秒，人工替代率 ≥ 50-70%，CSAT ≥ 90%
- **管理后台**：页面首屏加载 < 2秒，API 响应 < 500ms (P95)，峰值 QPS ≥ 1000

### 第二阶段：技术选型

#### 2.1 核心技术栈推荐

| 层级 | 组件 | 推荐方案 | 说明 |
|------|------|---------|------|
| **AI框架** | Agent编排 | LangChain / LangGraph | 主流Agent开发框架，支持工具调用与多轮对话 |
| **大语言模型** | LLM | DeepSeek / Qwen2-7B | 中文场景优先，可选云API或本地量化部署 |
| **向量数据库** | 检索 | Chroma（本地）/ Milvus（生产） | 存储知识库向量，支持语义检索 |
| **后端框架** | Web服务 | FastAPI（Python）/ Spring Cloud（Java） | Python生态首选FastAPI，Java生态用Spring Cloud |
| **实时通信** | 消息推送 | WebSocket | 支持双向实时对话 |
| **部署** | 容器编排 | Docker + Kubernetes | 生产环境高可用部署 |
| **监控** | 可观测性 | Prometheus + Grafana | 实时指标监控与告警 |

#### 2.2 架构模式选型

**管理后台**：采用 **FastAPI + SQLAlchemy 异步** 分层架构（Router → Service → Repository），所有管理接口统一前缀 `/api/admin/`。

**CS Agent 客服**（已有）：多Agent协同架构（Supervisor编排模式），由主管Agent统一调度多个专业子Agent。

> 管理后台与 CSagent 共享 PostgreSQL 数据库和 Service 层，客服工具调用将从模拟数据切换为真实数据库查询。

### 第三阶段：系统架构设计

#### 3.1 整体分层架构

```
┌──────────────────────────────────────────────────────────────────────┐
│                          前端接入层                                    │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐   │
│  │   管理后台前端     │  │   客服对话组件     │  │   C端商城(已完成) │   │
│  │  (Vue3+Element+) │  │   (WebSocket)     │  │   (Vue3+Element) │   │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘   │
└───────────┼─────────────────────┼─────────────────────┼──────────────┘
            │                     │                     │
┌───────────▼─────────────────────▼─────────────────────▼──────────────┐
│                       API Gateway 层 (FastAPI)                        │
│      JWT鉴权 / RBAC权限校验 / 限流熔断 / 请求日志 / CORS              │
├──────────────────────────────────────────────────────────────────────┤
│                         业务路由层                                     │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐        │
│  │/api/auth   │ │/admin/users│ │/admin/prod │ │/admin/order│        │
│  │  认证登录   │ │  用户管理   │ │  商品管理   │ │  订单管理   │        │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘        │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐        │
│  │/admin/inv  │ │/admin/pay  │ │/admin/ship │ │/admin/mkt  │        │
│  │  库存管理   │ │  支付管理   │ │  物流管理   │ │  营销管理   │        │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘        │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐        │
│  │/admin/rv   │ │/api/ticket │ │/api/chat   │ │/admin/rpt  │        │
│  │  评价管理   │ │  工单管理   │ │  CS客服     │ │  数据报表   │        │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘        │
│  ┌────────────┐                                                      │
│  │/api/health │  健康检查 / 就绪探针                                   │
│  └────────────┘                                                      │
└───────────────────────────────┬──────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────────┐
│                         Service 业务逻辑层                             │
│  AuthService  UserService  ProductService  OrderService              │
│  InventoryService  PaymentService  ShipmentService  CouponService    │
│  ReviewService  ReportService  TicketService (CSagent 共用)          │
└───────────────────────────────┬──────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────────┐
│                       Agent 核心层 (CSagent 已有)                      │
│  SupervisorAgent → KnowledgeAgent / OrderAgent / AfterSaleAgent      │
│                    → ComplianceAgent → 返回用户                        │
└───────────────────────────────┬──────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────────┐
│                           数据层                                      │
│   PostgreSQL 17(27张业务表) / Redis(缓存/会话) / Chroma(向量)         │
└──────────────────────────────────────────────────────────────────────┘
```

**分层职责**：

| 架构分层 | 对应目录 | 职责 |
|----------------|---------|------|
| 用户接入层 | `web/` | 管理后台前端 + 客服对话组件 + C端商城(已完成) |
| API Gateway层 | `app/api/` | JWT鉴权、RBAC权限、限流、路由分发 |
| 业务路由层 | `app/api/routes/` | 管理后台CRUD接口 + CS客服接口 |
| Service层 | `app/services/` | 核心业务逻辑、数据校验、事务管理 |
| Agent核心层 | `app/agents/` + `app/tools/` + `app/dialogue/` | CSagent 多Agent编排 |
| 数据层 | `app/knowledge/` + `app/core/` + `app/models/` | DB + Redis + Chroma |

#### 3.2 核心数据流

1. 用户提问 → API Gateway（鉴权、限流）
2. Supervisor Agent分析意图，路由至对应专业Agent
3. 专业Agent执行：检索知识库 / 调用电商业务API
4. 合规Agent审查回复内容
5. 汇总结果 → 流式返回用户

#### 3.3 安全设计要点

**写操作安全闭环**：涉及订单修改、退款等写操作时，采用"提案 → 用户确认 → 后端执行"的三段式闭环，**模型不直接修改业务数据库**。

#### 3.4 项目目录大纲设计

> 目录结构与分层架构一一对应，按「接入层 → 网关层 → Agent核心层 → 数据层」组织代码，便于团队协作与后续维护。

```
CSagent/
├── README.md                        # 项目说明（快速开始、环境要求、启动命令）
├── 流程设计.md                       # 本文档：全流程技术落地方案
├── requirements.txt                 # Python依赖清单（FastAPI/LangChain/Chroma等）
├── pyproject.toml                   # 项目元数据与依赖管理（可选，替代requirements）
├── .env.example                     # 环境变量模板（LLM_API_KEY、DB连接串等）
├── .gitignore
│
├── docs/                            # 项目文档
│   ├── requirements.md              # 第一阶段：需求文档（痛点、优先级、指标）
│   ├── architecture.md              # 第三阶段：架构设计详图与数据流说明
│   ├── api.md                       # 接口文档（对外HTTP/WebSocket协议定义）
│   └── runbook.md                   # 第七阶段：运维手册（告警处理、回滚预案）
│
├── app/                             # ============ 应用主代码 ============
│   ├── main.py                      # FastAPI入口：创建app、注册路由、中间件
│   │
│   ├── config/                      # 配置中心（对应：技术选型）
│   │   ├── settings.py              # 全局配置（pydantic-settings读取.env）
│   │   └── logging.py               # 日志配置（结构化日志，对接监控）
│   │
│   ├── api/                         # ---- API Gateway层 ----
│   │   ├── deps.py                  # 依赖注入（JWT鉴权、RBAC权限、会话初始化）
│   │   ├── middleware/              # 中间件
│   │   │   ├── auth.py             # JWT Token 验签 + RBAC 权限校验
│   │   │   ├── rate_limit.py        # Redis 分布式限流
│   │   │   └── access_log.py        # 请求日志 + 操作审计
│   │   └── routes/                  # 路由模块
│   │       ├── auth.py              # 认证登录：登录/注册/刷新Token
│   │       ├── chat.py              # CS Agent 对话接口（已实现）
│   │       ├── session.py           # 会话管理（已实现）
│   │       ├── ticket.py            # 工单接口（已实现）
│   │       ├── knowledge.py         # 知识库管理（已实现）
│   │       ├── health.py            # 健康检查（已实现）
│   │       ├── admin_users.py       # 🆕 用户管理：CRUD、封禁、地址管理
│   │       ├── admin_products.py    # 🆕 商品管理：商品CRUD、上/下架、SKU管理
│   │       ├── admin_orders.py      # 🆕 订单管理：列表/详情/状态流转/取消
│   │       ├── admin_inventory.py   # 🆕 库存管理：库存查询/调整/流水/预警
│   │       ├── admin_payments.py    # 🆕 支付管理：流水查询、退款审核
│   │       ├── admin_shipments.py   # 🆕 物流管理：发货、物流追踪
│   │       ├── admin_marketing.py   # 🆕 营销管理：优惠券/促销活动CRUD
│   │       ├── admin_reviews.py     # 🆕 评价管理：审核/回复/隐藏
│   │       └── admin_reports.py     # 🆕 数据报表：销售统计/用户分析/客服看板
│   │
│   ├── agents/                      # ---- Agent核心层（对应3.1核心层）----
│   │   ├── base.py                  # Agent基类（推理五阶段公共逻辑）
│   │   ├── supervisor.py            # 编排Agent：意图路由、任务分发、结果汇总
│   │   ├── knowledge_agent.py       # 知识检索Agent（RAG：召回→重排→生成）
│   │   ├── order_agent.py           # 订单处理Agent（订单/物流/支付API调用）
│   │   ├── aftersale_agent.py       # 售后工单Agent（退换货、工单流转）
│   │   ├── compliance_agent.py      # 合规审查Agent（回复内容审核过滤）
│   │   └── graphs/                  # LangGraph状态图定义
│   │       ├── state.py             # 全局会话状态（State Schema定义）
│   │       └── workflow.py          # Supervisor编排流程图（节点与边）
│   │
│   ├── tools/                       # 工具注册中心（对应4.2工具列表）
│   │   ├── registry.py              # Tool注册与发现机制
│   │   ├── product_tools.py         # search_product 商品搜索
│   │   ├── order_tools.py           # query_order 订单查询
│   │   ├── logistics_tools.py       # track_logistics 物流追踪
│   │   ├── policy_tools.py          # check_return_policy 退换货政策
│   │   └── ticket_tools.py          # create_ticket 创建工单
│   │
│   ├── dialogue/                    # 对话管理（对应4.3）
│   │   ├── intent.py                # 意图识别（微调BERT，12+类意图）
│   │   ├── slots.py                 # 槽位填充（FormAction引导补全信息）
│   │   ├── memory.py                # 多轮记忆（Session State、上下文补全）
│   │   └── handoff.py               # 人工转接（低置信度/复杂问题平滑兜底）
│   │
│   ├── services/                    # Service 业务逻辑层（全部从模拟替换为真实DB操作）
│   │   ├── gateway_client.py        # API Gateway统一客户端（重试/熔断）
│   │   ├── auth_service.py          # 🆕 认证：JWT签发、密码哈希、Token刷新
│   │   ├── user_service.py          # 🆕 用户：CRUD、地址管理、会员等级
│   │   ├── product_service.py       # 🆕 商品：从模拟替换为 PostgreSQL CRUD
│   │   ├── order_service.py         # 🆕 订单：从模拟替换为 PostgreSQL + 状态机
│   │   ├── payment_service.py       # 🆕 支付：从模拟替换为 PostgreSQL CRUD
│   │   ├── logistics_service.py     # 🆕 物流：从模拟替换为 PostgreSQL CRUD
│   │   ├── inventory_service.py     # 🆕 库存：库存扣减/释放/预警
│   │   ├── coupon_service.py        # 🆕 营销：优惠券发放/核销/过期
│   │   └── review_service.py        # 🆕 评价：审核/回复/敏感词过滤
│   │
│   ├── knowledge/                   # 知识库模块（对应4.1）
│   │   ├── pipeline/                # 数据处理流水线
│   │   │   ├── collector.py         # 步骤一：数据采集（历史对话/商品文档/FAQ）
│   │   │   ├── cleaner.py           # 步骤一：清洗去重、合并相似表述
│   │   │   └── splitter.py          # 步骤二：分块（512 token，重叠128）
│   │   ├── embedding.py             # 步骤三：向量化（BGE-large Embedding）
│   │   ├── vectordb.py              # 步骤三：向量库封装（Chroma/Milvus统一接口）
│   │   ├── retriever.py             # 检索器：Top-K召回 + Rerank重排序
│   │   └── updater.py               # 步骤四：热更新（Webhook/MQ监听变更）
│   │
│   ├── core/                        # 基础设施
│   │   ├── llm.py                   # LLM客户端
│   │   ├── redis_client.py          # Redis（多用途：会话/缓存/分布式限流）
│   │   ├── db.py                    # PostgreSQL连接（需更新：增加全部新表映射）
│   │   └── security.py              # 加解密、脱敏、JWT工具、密码哈希
│   │
│   └── models/                      # 数据模型（需大幅扩展）
│       ├── schemas.py               # Pydantic：现有客服模型 + 🆕 商品/订单/用户等
│       └── tables.py                # SQLAlchemy：现有3张表 + 🆕 24张新表
│
├── data/                            # 数据资产（知识库原始语料）
│   ├── raw/                         # 原始数据：客服对话记录、商品文档、FAQ
│   ├── processed/                   # 清洗后的标准问答对
│   └── eval/                        # 评估数据集（50+典型多轮场景用例）
│
├── scripts/                         # 运维与数据脚本
│   ├── init_db.py                   # 🆕 一键建全部27张表（替代原3张表版本）
│   ├── seed_data.py                 # 🆕 种子数据：类目/测试商品/测试用户/优惠券
│   ├── ingest_knowledge.py          # 知识库入库（清洗→分块→向量化→写Chroma）
│   ├── eval_dialogue.py             # 对话效果评估
│   └── export_logs.py               # 未解决问题导出 + 运营报表导出
│
├── tests/                           # 测试（对应第五阶段）
│   ├── unit/                        # 单元测试：工具函数、意图识别（覆盖率≥80%）
│   ├── integration/                 # 集成测试：检索链路、API调用链路
│   ├── dialogue/                    # 对话测试：多轮场景回归（准确率≥90%）
│   └── load/                        # 压力测试：JMeter脚本、大促并发模拟
│
├── deploy/                          # 部署（对应第六阶段）
│   ├── Dockerfile                   # 应用镜像构建
│   ├── docker-compose.yml           # 本地/轻量服务器一键部署（App+Redis+PostgreSQL+Chroma）
│   ├── k8s/                         # K8s清单：Deployment/Service/HPA弹性扩缩容
│   └── monitoring/                  # Prometheus规则 + Grafana仪表盘配置
│
└── web/                             # 前端接入层
    ├── chat-widget/                 # 客服对话组件（WebSocket，嵌入各页面）
    ├── admin/                       # ✅ 管理后台 SPA（Vue3 + Element Plus）
    │   ├── src/
    │   │   ├── views/              # 页面：Dashboard/商品/订单/用户/库存/营销/评价
    │   │   ├── components/         # 通用组件：表格/表单/图表/上传
    │   │   ├── router/             # 路由配置 + 权限守卫
    │   │   ├── stores/             # Pinia 状态管理
    │   │   └── api/                # Axios 封装 + API 接口定义
    │   └── package.json
    └── shop/                        # ✅ C端商城 SPA（Vue3 + Element Plus）
        ├── src/
        │   ├── views/              # 页面：首页/商品列表/商品详情/购物车/结算
        │   │                       #       订单列表/订单详情/个人中心/收货地址
        │   ├── components/         # ShopLayout 商城布局
        │   ├── router/             # 路由 + 登录守卫
        │   ├── stores/             # Pinia auth/cart 状态
        │   └── api/                # Axios API 接口（auth/products/orders/cart/user）
        └── package.json
```

**目录与架构的映射关系**：

| 架构分层 | 对应目录 | 职责 |
|----------------|---------|------|
| 用户接入层 | `web/` | 管理后台前端 + 客服对话组件 + C端商城(已完成) |
| API Gateway层 | `app/api/` | JWT鉴权、RBAC权限、限流、路由分发 |
| Service业务层 | `app/services/` | 核心业务逻辑、事务管理、数据校验 |
| Agent核心层 | `app/agents/` + `app/tools/` + `app/dialogue/` | CSagent多Agent编排（已有） |
| 数据层 | `app/knowledge/` + `app/core/` + `app/models/` | PostgreSQL + Redis + Chroma |
| 测试/部署/运维 | `tests/` + `deploy/` + `scripts/` | 测试/部署/运维支撑 |

**开发顺序建议**（新版9大阶段）：
1. `models/tables.py` + `scripts/init_db.py` → 建立全部27张表（第四阶段）
2. `services/` → 实现各Service层CRUD逻辑（第五阶段）
3. `api/routes/admin_*.py` + `api/routes/auth.py` → 暴露管理后台API（第五阶段）
4. `api/deps.py` + `api/middleware/auth.py` → JWT + RBAC 权限体系（第五阶段）
5. `agents/tools/` → 将CS Agent工具调用从模拟切换到真实Service（第六阶段）
6. `web/admin/` → 管理后台前端开发（第五阶段）
7. `scripts/seed_data.py` → 种子数据填充（第五阶段）
8. `tests/` → 测试验证（第七阶段）
9. `deploy/` → 部署上线（第八阶段）

## 三、新增：数据库全景设计（第四阶段）

> 现有 3 张 CSagent 表（conversations/messages/tickets）保持不动。以下为新增的 24 张业务表。

### 3.1 表关系总览

```
                       ┌──────────┐
                       │  users   │
                       └────┬─────┘
                            │
          ┌─────────────────┼──────────────────┐
          │                 │                  │
   ┌──────▼──────┐  ┌──────▼──────┐   ┌───────▼──────┐
   │  addresses  │  │   orders    │   │   reviews    │
   │  收货地址    │  │  订单表     │   │  评价表       │
   └─────────────┘  └──────┬──────┘   └──────────────┘
                           │
       ┌───────────────────┼───────────────────┐
       │                   │                   │
 ┌─────▼─────┐     ┌──────▼──────┐     ┌──────▼──────┐
 │order_items│     │  payments   │     │  shipments  │
 │ 订单明细   │     │  支付记录    │     │  物流记录    │
 └─────┬─────┘     └──────┬──────┘     └─────────────┘
       │                  │
 ┌─────▼─────┐     ┌──────▼──────┐
 │ products  │     │  refunds    │
 │ 商品表     │     │  退款记录    │
 └─────┬─────┘     └─────────────┘
       │
 ┌─────▼──────┐   ┌──────────┐   ┌──────────┐
 │   skus     │──►│inventory │   │categories│
 │  SKU表     │   │ 库存表    │   │ 类目表    │
 └────────────┘   └──────────┘   └──────────┘

营销: coupons / promotions / user_coupons
权限: roles / permissions / user_roles / operation_logs
```

### 3.2 分模块 DDL（24张新表概要）

**用户与权限（5张）**：`users`（含密码哈希/手机/邮箱/角色）、`addresses`（收货地址）、`roles`、`permissions`、`user_roles`
> RBAC设计：超级管理员 > 管理员 > 运营 > 只读，按 resource+action 粒度的权限矩阵

**商品与库存（7张）**：`categories`（三级类目树）、`products`（SPU+状态+搜索全文索引）、`product_images`、`skus`（规格+价格+条码）、`inventory`（可用库存+锁定库存+安全库存）、`inventory_logs`（出入库流水）
> 库存扣减走乐观锁 `UPDATE inventory SET quantity=quantity-? WHERE sku_id=? AND quantity>=?`

**订单管理（3张）**：`orders`（订单状态机+支付状态）、`order_items`（快照防止商品变更）、`order_logs`（操作审计）
> 订单状态流转：pending → confirmed → shipped → delivered → completed（可取消）

**支付（2张）**：`payments`（支付流水号+渠道+交易号）、`refunds`（退款编号+审批流程）

**物流（1张）**：`shipments`（物流单号+承运商+轨迹状态）

**营销（3张）**：`coupons`（满减/折扣模板）、`promotions`（秒杀/满减活动JSON规则）、`user_coupons`（领券记录+核销）

**评价（2张）**：`reviews`（1-5星+图片+商家回复）、`csat_scores`（客服满意度评价）

**系统管理（1张）**：`operation_logs`（模块/动作/对象/变更详情/ip，用于审计追溯）

### 3.3 全表清单

| # | 表名 | 模块 | 状态 |
|---|------|------|------|
| 1-3 | `conversations` `messages` `tickets` | CS客服 | ✅ 已有 |
| 4 | `write_proposals` | 安全闭环 | ⏳ 设计中 |
| 5 | `users` | 用户管理 | 🆕 |
| 6 | `addresses` | 收货地址 | 🆕 |
| 7 | `roles` | 角色定义 | 🆕 |
| 8 | `permissions` | 权限定义 | 🆕 |
| 9 | `user_roles` | 用户-角色关联 | 🆕 |
| 10 | `categories` | 商品类目 | 🆕 |
| 11 | `products` | 商品SPU | 🆕 |
| 12 | `product_images` | 商品图片 | 🆕 |
| 13 | `skus` | 商品SKU | 🆕 |
| 14 | `inventory` | 库存 | 🆕 |
| 15 | `inventory_logs` | 库存流水 | 🆕 |
| 16 | `orders` | 订单 | 🆕 |
| 17 | `order_items` | 订单明细 | 🆕 |
| 18 | `order_logs` | 订单日志 | 🆕 |
| 19 | `payments` | 支付记录 | 🆕 |
| 20 | `refunds` | 退款记录 | 🆕 |
| 21 | `shipments` | 物流 | 🆕 |
| 22 | `coupons` | 优惠券模板 | 🆕 |
| 23 | `promotions` | 促销活动 | 🆕 |
| 24 | `user_coupons` | 用户领券 | 🆕 |
| 25 | `reviews` | 商品评价 | 🆕 |
| 26 | `csat_scores` | 客服评价 | 🆕 |
| 27 | `operation_logs` | 操作日志 | 🆕 |

> 详细 DDL 见 `app/models/tables.py`（待实现）和 `scripts/init_db.py`（需更新为建全部27张表）。

---

## 四、管理后台开发（第五阶段）🆕

### 4.1 认证授权体系（基础先做）

| 功能 | 端点 | 说明 |
|------|------|------|
| 管理员登录 | `POST /api/auth/login` | 用户名+密码 → JWT access_token + refresh_token |
| 刷新Token | `POST /api/auth/refresh` | refresh_token → 新access_token |
| 修改密码 | `PUT /api/auth/password` | 需验证旧密码 |
| 当前用户信息 | `GET /api/auth/me` | 返回角色+权限列表 |

JWT 中间件：`app/api/middleware/auth.py`，RBAC 装饰器 `@require_permission("products", "delete")`

### 4.2 商品管理 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/admin/products` | GET | 商品列表（分页/搜索/类目筛选/状态筛选/排序） |
| `/api/admin/products` | POST | 创建商品（含SKU列表+图片） |
| `/api/admin/products/{id}` | GET | 商品详情（含SKU+图片+库存） |
| `/api/admin/products/{id}` | PUT | 更新商品 |
| `/api/admin/products/{id}` | DELETE | 软删除（下架） |
| `/api/admin/products/{id}/online` | PUT | 上架 |
| `/api/admin/products/{id}/offline` | PUT | 下架 |
| `/api/admin/categories` | GET/POST/PUT/DELETE | 类目CRUD |

### 4.3 订单管理 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/admin/orders` | GET | 订单列表（分页/状态筛选/时间范围/搜索） |
| `/api/admin/orders/{id}` | GET | 订单详情（含明细+支付+物流+日志） |
| `/api/admin/orders/{id}/confirm` | PUT | 确认订单 |
| `/api/admin/orders/{id}/ship` | PUT | 发货（填物流单号） |
| `/api/admin/orders/{id}/cancel` | PUT | 取消订单（含原因，释放库存） |
| `/api/admin/orders/{id}/complete` | PUT | 手动完成 |

**订单状态机**：`pending → confirmed → shipped → delivered → completed`，任一非终态可 cancel

### 4.4 其他管理 API

| 模块 | 核心端点 |
|------|---------|
| 用户管理 | `GET/POST/PUT /api/admin/users`，`PUT /users/{id}/ban`，地址CRUD |
| 库存管理 | `GET /api/admin/inventory`（含预警），`POST /adjust`（调整库存），`GET /logs`（流水） |
| 支付管理 | `GET /api/admin/payments`（流水查询），`POST /api/admin/refunds/{id}/approve\|reject` |
| 物流管理 | `GET/POST /api/admin/shipments`，`PUT /shipments/{id}/track` |
| 营销管理 | 优惠券CRUD，促销活动CRUD，`POST /coupons/{id}/grant` 发券 |
| 评价管理 | `GET /api/admin/reviews`，`PUT /reviews/{id}/approve\|reject\|reply` |
| 数据报表 | `GET /api/admin/reports/sales`（日/周/月），`GET /reports/cs-agent`（客服数据大屏） |

### 4.5 Service 层设计

全部 10 个 Service 类，核心实现要点：

| Service | 关键逻辑 |
|---------|---------|
| `AuthService` | bcrypt密码哈希、pyjwt签发/验证、refresh_token轮换 |
| `UserService` | CRUD+地址管理、`ban_user()` 封禁逻辑 |
| `ProductService` | 级联创建（产品+SKU+图片+库存初始化）、全文搜索（PostgreSQL tsvector） |
| `OrderService` | 状态机流转（乐观锁version防并发）、`cancel_order()` 释放库存 |
| `InventoryService` | 预占库存（下单锁）、确认扣减（支付后扣）、超时释放（15分钟未支付） |
| `CouponService` | 领券校验（库存+限领+有效期）、核销（计算最优优惠组合） |
| `OperationLogService` | 中间件自动记录（装饰器 `@audit_log(module, action)`） |

> 所有 Service 层的 async 方法使用 `async with db.Session()` 管理事务，外键约束确保数据一致性。

### 4.6 管理后台前端（`web/admin/`）

| 页面 | 功能 |
|------|------|
| Dashboard | 今日订单数/销售额/新增用户/待处理退款，ECharts趋势图 |
| 商品管理 | 表格+搜索+批量上下架，表单创建/编辑（SKU动态行+图片上传） |
| 订单管理 | Tab筛选状态，时间线组件展示状态流转，发货弹窗 |
| 用户管理 | 表格+封禁/解禁，收货地址查看 |
| 库存管理 | 库存预警高亮，调整弹窗，出入库流水时间线 |
| 营销中心 | 优惠券/活动列表，创建表单含时间选择器 |
| 评价管理 | 审核队列，商家回复编辑 |
| 客服看板 | 接入CSagent数据：转人工率/CSAT/热点问题 |
| 操作日志 | 按模块/时间/操作人筛选的审计日志表 |

> 技术栈：Vue3 + TypeScript + Element Plus + Pinia + ECharts + Axios

---

## 五、CS Agent 增强（第六阶段）

> 现有 CSagent 功能完整保留，本阶段仅做**工具调用从模拟切换到真实数据**。

### 5.1 工具切换清单

| 工具 | 当前状态 | 切换后 |
|------|---------|--------|
| `query_order` | 模拟返回硬编码数据 | 调用 `OrderService.get_order_detail()` 查 PostgreSQL |
| `track_logistics` | 模拟返回硬编码数据 | 调用 `LogisticsService.track_by_order()` 查 shipments 表 |
| `search_product` | 模拟返回硬编码数据 | 调用 `ProductService.search()` 全文搜索 products 表 |
| `check_stock` | 模拟返回硬编码数据 | 调用 `InventoryService.check()` 查 inventory 表 |
| `create_ticket` | 创建但未持久化 | 调用 `TicketService.create()` 写入 tickets 表 |
| `check_return_policy` | 内置静态文本 | 已有从 knowledge base 检索，可扩展为查 refunds 表历史 |

### 5.2 知识库增强

- 商品上下架时通过 Webhook 触发 Chroma 向量索引更新（已有 `updater.py` 框架）
- 知识库新增商品类问答：材质/规格/使用说明（从 products 表自动生成）

### 5.3 新增 Agent 工具（可选）

| 工具 | 功能 | 使用场景 |
|------|------|---------|
| `recommend_product` | 商品推荐 | "200元左右的龙井推荐" → 查 products 表按价格/类目筛选 |
| `check_coupon` | 优惠券查询 | "我有什么优惠券" → 查 user_coupons 表 |
| `calculate_discount` | 优惠计算 | "买两件怎么优惠" → 匹配 promotions 规则 |

---

## 六、技术选型速查表

| 场景 | 推荐方案 | 理由 |
|------|---------|------|
| **后端框架** | FastAPI（异步） | 生态成熟，自动OpenAPI文档 |
| **ORM** | SQLAlchemy 2.0 async | 成熟稳定，支持原生JSONB/全文搜索 |
| **数据库** | PostgreSQL 17 | 支持JSONB、tsvector全文搜索、窗口函数 |
| **认证** | JWT (pyjwt) + bcrypt | 无状态认证，适合管理后台 |
| **前端框架** | Vue3 + Element Plus | 中小团队首选，组件丰富 |
| **图表** | ECharts 5 | 数据看板和报表可视化 |
| **LLM调用** | 本地 llama.cpp / 云端 OpenAI | 已有，双模式切换 |
| **向量搜索** | Chroma（开发）/ Milvus（生产） | 已有 |
| **多Agent编排** | LangGraph | 已有，Supervisor模式已跑通 |
| **缓存** | Redis 7 | 会话/高频问答/分布式限流 |
| **部署** | Docker Compose（开发）/ K8s（生产） | 已有 docker-compose |

---

## 七、实施路线图

| 阶段 | 周期 | 内容 | 状态 |
|------|------|------|------|
| **第一阶段** | 已完成 | CSagent 骨架：Agent编排/RAG/意图识别/工具调用 | ✅ |
| **第二阶段** | 已完成 | 知识库构建：100条茶叶FAQ向量化入库 | ✅ |
| **第三阶段** | 第1-2周 | **数据库扩展**：建全部27张表 + `seed_data.py`种子数据 | 🆕 |
| **第四阶段** | 第3-6周 | **管理后台核心**：认证+商品+订单+库存+用户 CRUD | 🆕 |
| **第五阶段** | 第7-8周 | **管理后台扩展**：支付/物流/营销/评价 + 操作日志 | 🆕 |
| **第六阶段** | 已完成 | **管理后台前端**：Vue3 11个页面 + 数据看板（端口3000） | ✅ |
| **第七阶段** | 已完成 | **C端商城前端**：Vue3 10个页面（首页/商品/购物车/结算/订单/个人中心，端口3001） | ✅ |
| **第八阶段** | 第11-12周 | **CS Agent增强**：工具切换真实数据 + 知识库增强 | 🆕 |
| **第九阶段** | 第13-14周 | **测试与调优**：单元/集成/E2E测试 + 性能压测 | ⏳ |
| **第十阶段** | 第15-16周 | **部署上线**：灰度→全量 + 监控告警 + 文档 | ⏳ |

**总计：约4个月完成从CSagent骨架到完整电商后台管理系统的升级。**

---

## 八、关键注意事项

1. **写操作安全**：退款/改地址等操作必须经用户二次确认，Agent工具不直改数据库（走 write_proposals 三段式闭环）
2. **合规审查**：所有AI回复需经过 ComplianceAgent 审核，管理后台操作需 RBAC 权限校验
3. **人工兜底**：CSagent 低置信度/投诉场景平滑转接人工
4. **库存安全**：库存扣减走数据库乐观锁，防止超卖
5. **数据隐私**：用户手机号/地址加密存储，日志脱敏，支持GDPR用户数据删除
6. **操作审计**：所有管理后台操作记录到 `operation_logs`，不可篡改
7. **成本控制**：高频客服问答 Redis 缓存，减少 LLM 调用
